space: input
imports: ['../typography.yaml']

native:
  base:
    background-color: transparent
    ref: $typography.text.base
    color: $color.primary.brand.primary
    border: none
    outline: none
    transition: all .2s
    box-sizing: border-box
    width: 100%
    height: 100%
  design:
    material:
      padding: 0
  size:
    xs:
      font-size: $typography.b.b1.size
      line-height: $typography.b.b1.height
    s:
      font-size: $typography.b.b2.size
      line-height: $typography.b.b2.height
    m:
      font-size: $typography.b.b2.size
      line-height: $typography.b.b2.height
    l:
      font-size: $typography.b.b2.size
      line-height: $typography.b.b2.height
    xl:
      font-size: $typography.b.b3.size
      line-height: $typography.b.b3.height
    xxl:
      font-size: $typography.b.b3.size
      line-height: $typography.b.b3.height

placeholder:
  base:
    width: 100%
    ref: $typography.text.base
    color: $color.primary.seattle.c60
    line-height: auto
  disabled:
    color: $color.primary.london.c120
  gap:
    top: $layout.size.s3

wrapper:
  base:
    background-color: $color.primary.brand.white
    transition: all .2s
    border-radius: $border.radius.m
    width: 100%
    color: $color.primary.brand.primary
    padding-left: $layout.size.s3
    padding-right: $layout.size.s3
  design:
    outline:
      border: $border.width.s solid $color.primary.seattle.c30
    material:
      border-bottom: $border.width.s solid $color.primary.seattle.c30
      border-radius: 0
      padding: 0
  size:
    xs:
      height: $layout.size.s6
      border-radius: $border.radius.s
      padding-left: $layout.size.s2
      padding-right: $layout.size.s2
    s:
      height: $layout.size.s8
    m:
      height: $layout.size.s9
    l:
      height: $layout.size.s10
    xl:
      height: $layout.size.s12
    xxl:
      height: $layout.size.s13
  hover:
    outline:
      border-color: $color.primary.brand.primary
      box-shadow: inset 0px 0px 0px 1px $color.primary.brand.primary
    material:
      border-bottom-color: $color.primary.brand.primary
      box-shadow: inset 0px -1px 0px 0px $color.primary.brand.primary
  disabled:
    outline:
      pointer-events: none
      border-color: $color.primary.brand.wildsand
      background-color: $color.primary.brand.wildsand
      color: $color.primary.london.c120
    material:
      pointer-events: none
      border-bottom-color: $color.primary.brand.wildsand
      color: $color.primary.london.c120
  invalid:
    outline:
      border-color: $color.utilitarian.moscow.c100
    material:
      border-bottom-color: $color.utilitarian.moscow.c100

wrapped:
  base:
    color: $color.primary.brand.primary
  disabled:
    color: $color.primary.london.c120

infix:
  base:
    color: $color.primary.seattle.c60
  hover:
    color: $color.primary.brand.primary
  size:
    xs:
      margin: 0 $layout.size.s1
      font-size: $typography.b.b1.size
    s:
      margin: 0 $layout.size.s2
      font-size: $typography.b.b2.size
    m:
      margin: 0 $layout.size.s2
      font-size: $typography.b.b2.size
    l:
      margin: 0 $layout.size.s2
      font-size: $typography.b.b2.size
    xl:
      margin: 0 $layout.size.s3
      font-size: $typography.b.b3.size
    xxl:
      margin: 0 $layout.size.s3
      font-size: $typography.b.b3.size

prefix:
  base:
    margin-left: 0

postfix:
  base:
    margin-right: 0
